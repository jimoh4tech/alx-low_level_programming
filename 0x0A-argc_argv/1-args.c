#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vecctor arrray
 *
 * Return: 0;
 */

int main(int argc, __attribute__ ((unused)) char *argv[])
{
	printf("%d\n", (argc - 1));
	return (0);
}
